{
  "tasks": {
    // todo: exclude going into node_modules folders once we add support for globbing
    "test": "deno test -A",
    "build": "deno run -A --no-check --unstable https://deno.land/x/wasmbuild@0.8.1/main.ts --out lib/pkg"
  },
  "fmt": {
    "files": {
      "exclude": [
        "target/",
        "wasm/target/"
      ]
    }
  },
  "lint": {
    "rules": {
      "exclude": [
        "no-explicit-any",
        "camelcase"
      ]
    },
    "files": {
      "exclude": [
        "target/",
        "wasm/target/",
        "lib/pkg/",
        "rs-lib/src/polyfills/scripts/"
      ]
    }
  }
}
